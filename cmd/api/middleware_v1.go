package main

import "net/http"

// authenticateV1 is a placeholder. Full implementation in middleware_v1.go (Step 23).
func (app *application) authenticateV1(next http.Handler) http.Handler {
	return next
}
