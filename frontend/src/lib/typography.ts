/** Shared caption treatment for a sidebar section-group label (e.g. "EXPLORER",
 *  "HISTORY", "ACCESS CONTROL"). Used by both the IDE shell (`SidebarPane`) and
 *  the org/workspace/admin shells (`AppShellNavSection`) so every sidebar in the
 *  app renders section labels identically. */
export const sectionCaptionClass =
  'text-[10px] font-semibold uppercase tracking-wider text-muted-foreground'
