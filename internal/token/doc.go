// Package token issues and verifies HS256-signed JWT access tokens that carry
// the account, auth session, and authentication method, and generates opaque
// random refresh tokens whose SHA-256 hashes are what the application stores.
package token
