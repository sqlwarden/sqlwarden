// Package database opens the SQLWarden application database and provides the
// Bun models and query helpers for its metadata: accounts, organizations,
// workspaces, environments, connections, RBAC roles and policies, sessions,
// settings, and workspace files. It also owns query logging and the migration
// lock.
package database
