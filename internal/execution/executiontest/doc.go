// Package executiontest contains runtime contract tests shared by the local and
// remote execution adapters, so LocalRuntime and WorkerRuntime are proven
// against the same session behavior.
package executiontest
