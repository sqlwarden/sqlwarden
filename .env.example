# Application Configuration
BASE_URL=http://localhost:6020
HTTP_PORT=6020

# Database Configuration
POSTGRES_USER=sqlwarden
POSTGRES_PASSWORD=sqlwarden_password
POSTGRES_DB=sqlwarden

# Security Keys - CHANGE THESE IN PRODUCTION!
# Generate random keys using: openssl rand -hex 16
COOKIE_SECRET_KEY=cpcgzjcote6h5hakeglpbzixhbuog2zc
JWT_SECRET_KEY=fb57i5hiud5mzmykaquqsln5gcmolbac

# Email Notifications (Optional)
# Set to an email address to receive error notifications
NOTIFICATIONS_EMAIL=

# SMTP Configuration (Optional)
# Required if NOTIFICATIONS_EMAIL is set
SMTP_HOST=example.smtp.host
SMTP_PORT=25
SMTP_USERNAME=example_username
SMTP_PASSWORD=pa55word
SMTP_FROM=SQLWarden <no_reply@example.org>
