# Application Configuration
BASE_URL=http://localhost:6020
HTTP_PORT=6020
LOG_FORMAT=json

# Database Configuration
POSTGRES_USER=sqlwarden
POSTGRES_PASSWORD=sqlwarden_password
POSTGRES_DB=sqlwarden

# File Storage Configuration
FILES_ROOT_DIR=/var/lib/sqlwarden/files

# Security keys - CHANGE THESE IN PRODUCTION!
# Generate independent values using: openssl rand -hex 32
COOKIE_SECRET_KEY=cpcgzjcote6h5hakeglpbzixhbuog2zc
JWT_SECRET_KEY=fb57i5hiud5mzmykaquqsln5gcmolbac
ENCRYPTION_KEY=dev-insecure-key-32byteslong!!

# SMTP and notification settings are managed at runtime through the
# administration API; they are not bootstrap environment variables.
